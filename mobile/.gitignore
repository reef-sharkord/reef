# Anchored to the top level so the generated Capacitor project is ignored but
# version-controlled plugin native source under plugins/**/android is kept.
node_modules/
/www/
/android/
/ios/
# Firebase app config (per-project; drop yours here to enable FCM). Keep private.
/google-services.json
