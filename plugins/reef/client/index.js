// REEF companion plugin — client entry.
//
// The GIF feature has no plugin UI: the REEF client provides its own picker and
// calls the server `searchGifs` action directly. This file exists so the plugin
// has a client half; future features (e.g. presence) may add slot components.

export const components = {};
